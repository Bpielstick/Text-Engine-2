{
  "name": "text-engine-2",
  "version": "1.0.0",
  "description": "A text-based RPG engine that loads content from external files and runs it in a GUI with clickable choices.",
  "main": "index.js",
  "scripts": {
    "build": "tsc && esbuild dist/index.js --bundle --outfile=public/bundle.js",
    "test": "mocha -r ts-node/register tests/**/*.spec.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "latest",
    "@typescript-eslint/parser": "latest",
    "chai": "latest",
    "eslint": "latest",
    "esbuild": "latest",
    "mocha": "latest",
    "typescript": "latest",
    "@types/node": "latest"
  }
}
